'use strict';

/**
 * @ngdoc function
 * @name contratempoApp.controller:SigninCtrl
 * @description
 * # SigninCtrl
 * Controller of the contratempoApp
 */
angular.module('contratempoApp')
    .controller('SignCtrl', function(
    ) {

    });
